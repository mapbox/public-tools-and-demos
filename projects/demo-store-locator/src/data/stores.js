const stores = {
  //  This demo utilizes a custom tileset where a large list of thousands of geojson features 
  // have been uploaded and processed into a tileset which was then added to the map style.
  // Features are queried with `queryRenderedFeatures()` in Map/index.jsx
}